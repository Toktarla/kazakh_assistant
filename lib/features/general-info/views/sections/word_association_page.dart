import 'package:flutter/material.dart';

class WordAssociationPage extends StatelessWidget {
  const WordAssociationPage({super.key});
  @override
  Widget build(BuildContext context) => Scaffold(
    appBar: AppBar(title: const Text('Word Association')),
    body: const Center(child: Text('Word Association Exercise')),
  );
}
