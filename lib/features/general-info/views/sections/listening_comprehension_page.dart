import 'package:flutter/material.dart';

class ListeningComprehensionPage extends StatelessWidget {
  const ListeningComprehensionPage({super.key});
  @override
  Widget build(BuildContext context) => Scaffold(
    appBar: AppBar(title: const Text('Listening Comprehension')),
    body: const Center(child: Text('Listening Comprehension Exercise')),
  );
}