import 'package:flutter/material.dart';

class TrueFalsePage extends StatelessWidget {
  const TrueFalsePage({super.key});
  @override
  Widget build(BuildContext context) => Scaffold(
    appBar: AppBar(title: const Text('True or False')),
    body: const Center(child: Text('True or False Exercise')),
  );
}